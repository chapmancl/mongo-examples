# All webui dependencies (flask, flask-cors, gunicorn, requests, pydantic) are
# provided by mongomcp[agent] installed in Dockerfile.base.
# Add any webui-specific packages here that are NOT part of mongomcp[agent].
--index-url=https://packages.lmig.com/api/pypi/python/simple
flask==3.1.3
flask-cors
gunicorn==23.0.0
pytest==8.3.2
coverage==7.6.1
requests
boto3>=1.34
botocore>=1.34
motor
mcp[cli]==1.26.0
pymongo
pydantic
pyJWT>=2.8
fastapi[standard]
fastmcp >=2.14
voyageai
asyncio-mqtt